/*
const sorting = function (x, y) {
  const xId = Number.parseInt(x.id.substr(1));
  const yId = Number.parseInt(y.id.substr(1));
  return xId - yId;
};

[...document.querySelectorAll("*[id^=c]")]
  .sort(sorting)
  .reverse()
  .forEach(x => text.appendChild(x));
*/
